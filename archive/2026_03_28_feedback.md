# Deborah Feedback Log - 2026-03-28

This note records Deborah's manuscript feedback, the current manuscript response in `index.qmd`, and any deferred follow-up. It preserves Deborah's comments verbatim, keeps the main response options visible, and separates implemented manuscript revisions from later analytic or model-development work.

All original Deborah points remain represented, with the two parameter-count comments consolidated in Item 4.

## 1. "Emotional associations" terminology

**Deborah feedback:** "Emotional associations link each item to its category's context pole: emotional items associate with the emotional pole and neutral items with the neutral pole." --> these should be semantic associations, i'm not sure what 'pole' means here?

**Manuscript locus:** eCMR Specification - emotional-context and emotion-feature wording (`index.qmd` L216-220, L264-279).

**Assessment:** The concern is well taken. The earlier wording made a category-coded emotional-context mechanism sound like semantic association and added undefined jargon through `pole`. The underlying problem was not semantic theory, but reader-facing ambiguity about what the model represents.

**Response options:**
1. `Source-context framing`: describe these as item bindings to category-specific source-context states.
2. `Category-context framing`: use plainer manuscript-specific wording such as category context or category state.
3. `Literal semantic framing`: adopt Deborah's wording directly and describe the associations as semantic.

**Current disposition:** Implemented in the manuscript with `category-context framing`. The eCMR specification now defines a binary emotion feature vector, removes `pole`, and describes emotional and neutral items as cueing different emotional-context states rather than semantic associations.

## 2. Output length conditioning

**Deborah feedback:** "which is appropriate because our primary behavioral outcomes and benchmarks target item selection rather than stopping behavior." -> normally before meeting you I would think that people stop because they can't remember any more items. This sentence isn't really clear to me still. I'm not sure what is the difference between the two options - isn't output length the same as 'how many'? "Conditioning on observed output length isolates the model's predictions about which items are recalled from predictions about how many"

**Manuscript locus:** eCMR Specification and Evaluation Approach - recall termination and set-likelihood framing (`index.qmd` L309-311, L351-379).

**Assessment:** The concern is well taken. The issue is not only prose but modelling scope: the current analysis treats recall stopping as outside the model even though stopping in free recall may depend on retrieval state. At the same time, an explicit stopping model would not automatically improve the present manuscript, because a position-only rule would add parameters without advancing the central mechanistic comparison, and a support-ratio rule is not yet validated enough to anchor the paper.

**Response options:**
1. `Motivated fixed output length`: retain conditioning on the number of items recalled on that list, but rewrite the passage so it explains why stopping is omitted and states the omission as a limitation.
2. `Future stopping extension`: add a brief note that a validated state-dependent stopping model would be a natural later extension, without making it part of the current manuscript's main analysis.
3. `Full sequence likelihood later`: pursue exact observed-sequence fitting only if true recall order can be validated or recovered from source data.

**Current disposition:** Implemented in the manuscript as a scope choice. The text now states that recall termination is not modeled explicitly, that simulations are conditioned on the number of items recalled on each list, and that stopping may nevertheless depend on retrieval state in free recall.

**Status:** Implemented in manuscript; methodological follow-up deferred.

**Deferred work:** Add a validated state-dependent stopping policy only if a later project branch wants stopping to depend on retrieval state directly. Exact sequence-likelihood fitting remains contingent on validating or recovering true recall order, because the stored TalmiEEG recall vectors are still consistent with sorted recalled positions rather than observed production order.

## 3. "Within-neutral semantic substructure"

**Deborah feedback:** "The model architecture represents a binary emotional-vs-neutral distinction and does not capture within-neutral semantic substructure" --> The architecture is about emotional experience, not semantics, this needs to be clarified since there's also semantic substructure within emotional. it simply does not capture the semantic associations. I wonder whether this comment is related to the mistake about semantic vs emotional associations above.

**Manuscript locus:** Model Variants - semantic scope statement (`index.qmd` L315-317).

**Assessment:** The concern is well taken. The limitation is not `within-neutral` semantic structure in particular; it is the absence of pre-experimental semantic similarity more generally. The earlier wording implied a category-specific omission when the actual omission is broader.

**Response options:**
1. `Text-only limitation`: simply rewrite the sentence to say the model lacks semantic structure for all items.
2. `Stronger source-vs-semantics rewrite`: explicitly contrast represented source-context structure with absent pre-experimental semantic similarity.
3. `Semantic-model extension`: broaden the project and add semantic structure to the modelling pipeline.

**Current disposition:** Implemented in the manuscript with the `text-only limitation` option. The Model Variants section now states that the model does not represent pre-experimental semantic similarity among studied items and that collapsing semantically related and unrelated neutral items follows from that scope.

**Status:** Implemented in manuscript; semantic-model extension deferred indefinitely..

**Deferred work:** A future semantic-model branch could add pre-experimental semantic structure directly rather than collapsing semantically related and unrelated neutral items for the present manuscript. But without more detailed semantic data for the current stimuli, that extension would require new assumptions and parameters that are not yet justified by the current dataset or the central mechanistic comparison.

## 4. Parameter-count notation unclear

**Deborah feedback:** "k=9, k=10... ? not sure what that means. is this the same as KL in the next part?" and "At k=10, the comparison between CMR+LPP and eCMR" --> again not sure how to read the k notation.

**Manuscript locus:** Model Variants and pooled comparison prose (`index.qmd` L320-347, L472-489).

**Assessment:** The concern is well taken. This item consolidates Deborah's two parameter-count comments, both of which pointed to the same reader-facing notation failure. Seeing `k = 9`, `k = 10`, and `\kappa_L` in close proximity created unnecessary visual competition at exactly the point where the prose should read most cleanly.

**Response options:**
1. `Keep k with definition`: define `k` on first use and in the table caption.
2. `Rename symbol`: switch from `k` to another count symbol such as `p`.
3. `Spell out in prose`: say "9 free parameters," "10 free parameters," and reserve a symbol only for tables if needed.

**Current disposition:** Implemented in the manuscript with `spell out in prose`. Model-variant prose and the equal-parameter comparison now use explicit free-parameter counts, while `k` is retained only where it remains useful in tables and captions.

## 5. `\phi_emot` "added" but already appeared

**Deborah feedback:** it adds the emotional context layer described above with one additional parameter controlling emotion-modulated encoding strength: --> the same parameter appears in the above equation, so I don't know how to read the statement that this now adds this parameter

**Manuscript locus:** eCMR Specification and Model Variants - emotional encoding strength and eCMR lead sentence (`index.qmd` L294-299, L330-334).

**Assessment:** The concern is well taken. The earlier narrative sounded self-contradictory by presenting the parameter conceptually in the specification and then sounding as though it was being introduced for the first time in the eCMR variant description.

**Response options:**
1. `Callback wording`: keep the earlier conceptual introduction and rephrase the later passage as a reminder or operationalization.
2. `Delay symbol introduction`: remove the symbol from the Introduction and introduce it only in Model Variants.
3. `Rephrase model comparison sentence`: retain both mentions but rewrite the later sentence so it no longer says eCMR "adds" the parameter.

**Current disposition:** Implemented in the manuscript with `callback wording`. The eCMR lead sentence now reads as a parameterization of the already-defined `\phi_{emot,i}` within the emotional-context architecture rather than as a second introduction of `\phi_{emot}`.

## 6. LPP restricted to emotional items - is this circular?

**Deborah feedback:** LPP-eCMR : if you had LPP modulate all trials AND have the emotion labels, I assume this model will do less well than if you force LPP to only modulate the emotional items, right? This part seems a bit dodgy

**Manuscript locus:** Model Variants - LPP-eCMR specification and limitation statement (`index.qmd` L336-342).

**Assessment:** The circularity concern is well taken. The present manuscript uses the earlier GLMM result to motivate an emotional-only LPP term and then evaluates the resulting model on the same dataset. That can be defended as a hypothesis-driven choice, but not as though the model independently discovered the restriction.

**Response options:**
1. `Hypothesis-driven reframing`: present the emotional-only LPP term as an explicit implementation of the earlier GLMM finding.
2. `New general-LPP comparison`: fit a matched variant where LPP can affect all items and report whether the restriction helps.
3. `Limitation-only acknowledgment`: keep the model unchanged but state plainly that the restriction is data-informed and partly circular.

**Current disposition:** Implemented in the manuscript with `hypothesis-driven reframing`. LPP-eCMR is now described as a hypothesis-driven extension that operationalizes the earlier GLMM result, and the manuscript explicitly states that a matched variant allowing LPP to affect all items was not tested.

**Status:** Implemented in manuscript; stronger empirical test deferred.

**Deferred work:** A matched general-LPP variant remains the cleaner empirical test if a later analysis wants to determine whether the emotional-only restriction is justified within the generative framework itself.

## 7. "Trial" ambiguous

**Deborah feedback:** "is fit to all 342 trials across 38 subjects." --> 'trial' here is a list? might want to be clearer.

**Manuscript locus:** Evaluation Approach - list-level unit of analysis (`index.qmd` L351-379).

**Assessment:** The concern is well taken. In this part of the manuscript, `trial` referred to the recalled-set unit, not to single-item EEG measurements. Because the paper also uses `trial-level` language for item-level LPP quantities, the earlier wording forced the reader to reparse the level of analysis.

**Response options:**
1. `Keep trial and define`: define `trial` on first use in this section.
2. `Use study-test list`: switch to the longer but precise phrase once, then shorten later.
3. `Use list consistently`: replace `trial` with `list` throughout this section.

**Current disposition:** Implemented in the manuscript by using `study-test list` on first list-level mention and `list` thereafter for the recalled-set unit. Item-level EEG passages continue to use `trial-level` wording where that meaning is correct.

## 8. "Different question ... not ... different noise levels"

**Deborah feedback:** "They answer a different question from pooled fits rather than the same question at different noise levels" --> please clarify

**Manuscript locus:** Evaluation Approach - pooled versus subject-wise inferential target (`index.qmd` L370-375).

**Assessment:** The concern is well taken. The earlier sentence asserted a distinction without stating what the two questions actually were. In this manuscript, pooled fits concern shared population-level parameterization, whereas subject-wise fits concern heterogeneity and participant-specific accommodation.

**Response options:**
1. `Keep abstract phrasing`: leave the sentence conceptually dense and trust the citation to carry it.
2. `Expand the sentence`: explicitly state the pooled-fit and subject-wise-fit inferential targets.
3. `Split into two sentences`: first define the pooled question, then define the subject-wise question.

**Current disposition:** Implemented in the manuscript by making the inferential targets explicit. The paragraph now says that pooled fits ask whether a single shared parameter vector predicts recall sets at the population level, whereas subject-wise fits ask how performance varies when each participant has a separate parameter vector.

## 9. "Floor the child model's per-subject NLL"

**Deborah feedback:** "we floor the child model's per-subject NLL" ??

**Manuscript locus:** Evaluation Approach - nested subject-wise comparison correction (`index.qmd` L374-375).

**Assessment:** The concern is well taken. `Floor` was jargon, and the earlier sentence assumed that the reader would reconstruct the nested-model logic without being told what practical problem the correction was solving.

**Response options:**
1. `Define the jargon`: keep `floor` and explain it.
2. `Replace with plain language`: describe optimizer failure and value replacement without jargon.
3. `Move details to note`: shorten the main text and relocate the full explanation to a footnote or supplement.

**Current disposition:** Implemented in the manuscript with `replace with plain language`. The sentence now explains first that occasional optimizer failures can make the more flexible nested model look spuriously worse for a given subject, and then states that its negative log-likelihood was replaced with the parent model's value before comparison.

## 10. "LPP will need to correlate with memory" - still true?

**Deborah feedback:** "To be useful for the purpose of model constraint the LPP will need to correlate with memory performance." --> I wonder if this sentence still true

**Manuscript locus:** EEG Results - `LPP and Emotion Enhanced Memory` (`index.qmd` L438-456).

**Assessment:** The concern is well taken. The earlier sentence was too broad, and the paragraph immediately followed it with null between-subject correlations, which made the premise sound self-refuting. The subsection needed a level-specific frame rather than a blanket claim.

**Response options:**
1. `Broad claim`: keep the general wording and let later results qualify it.
2. `Level-specific claim`: say the LPP must relate to memory at some relevant level of analysis, especially the trial level.
3. `Paragraph restructure`: rewrite the paragraph so it introduces multiple possible levels of useful association before reporting results.

**Current disposition:** Implemented in the manuscript with `level-specific claim` plus `paragraph restructure`. The subsection now says that usefulness depends on a relevant level of analysis and then proceeds through the between-subject, within-subject, and single-trial analyses in that order.

## 11. "Confirms the dual-context mechanism" - overstated?

**Deborah feedback:** "confirming that the dual-context mechanism captures meaningful structure in emotional recall beyond temporal dynamics alone." --> I wonder if this really confirms the dual-context mechanism? because don't you have, here, both two contexts, AND a modulation just of the emotional items by phi_emot? I suppose there could have been two contexts but no modulation (in the psych rev paper this is simulation 1 I think)

**Manuscript locus:** eCMR Model Comparison - Pooled Analysis and Qualitative Diagnostics (`index.qmd` L474-476, L518-520, L532-534).

**Assessment:** The concern is well taken. The current comparison does not isolate dual context alone; it compares single-context models against an eCMR package that includes emotional context together with category-based encoding modulation. Once that distinction is noticed, the same over-attribution can recur anywhere the manuscript treats the result as though it identified emotional-context architecture alone.

**Response options:**
1. `Combined mechanism claim`: attribute the result to the eCMR specification actually tested, not to dual context alone.
2. `New isolating comparison`: add a model comparison that holds encoding modulation fixed while varying only context architecture.
3. `Minor caveat`: keep the dual-context wording but append a short qualification about `\phi_emot`.

**Current disposition:** Implemented in the manuscript with `combined mechanism claim`. The pooled-analysis and qualitative-diagnostics prose now says that the results support the `eCMR specification tested here` rather than attributing them to dual context or emotional-context architecture alone.

**Status:** Implemented in manuscript; architecture-isolating comparison deferred.

**Deferred work:** A later comparison that holds encoding modulation fixed while varying only context architecture would be needed for a stronger architecture-specific claim.

## 12. "Untrained neural-behavioral summary statistic"

**Deborah feedback:** don't know how to read this: "The two models also make different predictions about an untrained neural-behavioral summary statistic."

**Manuscript locus:** Qualitative Diagnostics - second diagnostic setup (`index.qmd` L522-523).

**Assessment:** The concern is well taken. The phrase was unnecessarily compressed and imported machine-learning language that was doing little work in this paragraph. Once the empirical pattern is stated directly, the abstract setup line adds more decoding burden than value.

**Response options:**
1. `Keep the concept, define the phrase`: retain `untrained neural-behavioral summary statistic` and unpack it.
2. `Replace with plain language`: directly name the pattern that was not used during fitting.
3. `Cut the abstract setup`: remove the generic sentence and move straight to the empirical pattern.

**Current disposition:** Implemented in the manuscript with `cut the abstract setup`. The paragraph now moves directly from the diagnostic lead-in to the empirical recalled-versus-unrecalled Early LPP pattern.

## 13. "Consistent with the GLMM" is circular

**Deborah feedback:** "Empirically, recalled emotional items had higher Early LPP amplitudes than unrecalled emotional items; no such difference existed for neutral items, consistent with the GLMM." isn't the GLMM simply a description of the empirical data, not sure why it s repeated here or what the last phrase is meant to convey

**Manuscript locus:** Qualitative Diagnostics - empirical LPP pattern sentence (`index.qmd` L523).

**Assessment:** The concern is well taken. The earlier clause sounded like an independent validation even though it referred back to the same dataset-level pattern. Once the diagnostic paragraph was simplified, the GLMM clause became unnecessary.

**Response options:**
1. `Delete the phrase`: state the empirical pattern with no trailing comparison.
2. `Simple cross-reference`: say the pattern replicates the EEG result reported earlier.
3. `Keep GLMM reference`: retain the GLMM mention but rewrite it less strongly.

**Current disposition:** Implemented in the manuscript with `delete the phrase`. The empirical pattern now stands on its own without a trailing GLMM comparison.

## 14. "Genuine generalization" needs expansion

**Deborah feedback:** To expand for clarity: "This outcome reflects genuine generalization since the models are fit to set likelihood, not to the recalled-versus-unrecalled LPP statistic."

**Manuscript locus:** Qualitative Diagnostics - fitting target versus derived pattern (`index.qmd` L527-530).

**Assessment:** The concern is not a rejection of the claim so much as a request for the logic to be spelled out. The key point is that the model was fit to recalled sets, not to the recalled-versus-unrecalled LPP split. That supports a real but limited generalization claim, and the limitation matters because the model form itself was informed by prior empirical results.

**Response options:**
1. `Strong claim`: keep "genuine generalization" and add a fuller explanation.
2. `Scoped claim`: keep the idea but explicitly qualify what the generalization is relative to.
3. `Drop the claim`: describe the result without using generalization language.

**Current disposition:** Implemented in the manuscript with `scoped claim`. The qualitative-diagnostics section now states that the recalled-versus-unrecalled Early LPP separation was not directly fit, and that LPP-eCMR generalizes beyond the fitting target only in that limited sense.

## 15. Why does LPP only predict recall for emotional items?

**Deborah feedback:** Key curiosity - why LPP only adds to emotional items.

**Manuscript locus:** Model Variants and eCMR Model Comparison - emotional-only LPP restriction and interpretive bridge (`index.qmd` L340-342, L532-536).

**Assessment:** This feedback asks for a theoretical account rather than a local wording fix. The manuscript uses the emotional-only LPP effect in the model specification, but without some interpretation the restriction can feel purely stipulative. At the same time, a standalone Discussion paragraph would over-weight an interpretation that remains provisional.

**Response options:**
1. `Cautious interpretive bridge`: add 1-2 sentences near the end of the model-comparison interpretation arguing that the LPP may track affective significance or motivated attention more strongly for emotional items.
2. `Open question`: state that the current data do not explain the emotional-item specificity and leave the issue unresolved.
3. `Analysis gap`: treat the issue as motivation for new model comparisons or new empirical tests before advancing an account.

**Current disposition:** Implemented in the manuscript with `cautious interpretive bridge`. The model-comparison synthesis now adds a short interpretation that trial-to-trial Early LPP variation may be more behaviorally meaningful for emotional items because it more strongly indexes affective significance or motivated attention during encoding, while also stating that a weaker neutral-item effect could be too small or too noisy to detect here.

**Status:** Implemented as a cautious interpretation; mechanism remains open.

**Deferred work:** Further mechanistic analyses would be needed to determine why the LPP-memory relationship appears specific to emotional items in the current data.

## Cross-item synthesis

- The source-context versus semantics confusion showed up in multiple places. Items 1 and 3 were resolved by replacing semantic-sounding wording with explicit emotional-context and emotion-feature language.
- Evaluation-approach issues clustered around scope and level of analysis. Items 2, 7, 8, 9, and 10 were handled by clarifying the recalled-set unit, the pooled versus subject-wise comparison, and the fact that stopping is omitted rather than explained away.
- Mechanism-claim issues required narrower interpretation rather than broader analysis in the manuscript itself. Items 6, 11, 14, and 15 were handled by limiting claims to the eCMR specification actually tested, distinguishing fitting targets from derived patterns, and adding only a small amount of interpretation where the manuscript needed it.
- Most remaining open branches are methodological rather than editorial. The current manuscript revisions are primarily about clarity, claim discipline, and scope control.

## Current manuscript revision state

### Implemented manuscript revisions

- Items 1-6 were addressed through changes to the eCMR specification and model-variant descriptions.
- Items 7-10 were addressed through revisions to the Evaluation Approach and EEG Results framing.
- Items 11-15 were addressed through revisions to the pooled-comparison, qualitative-diagnostics, and model-comparison synthesis prose.
- The note is now aligned with the current wording in `index.qmd` rather than with earlier working recommendations.

### Remaining interpretive or methodological boundaries

- The manuscript now states several limitations explicitly rather than resolving them analytically in the current draft.
- The central remaining open branches concern stopping, semantic structure, general-LPP alternatives, architecture-isolating comparisons, and emotional-item specificity.

## Deferred analysis and model-development work

- Item 2: add a validated state-dependent stopping model if a later project branch wants stopping to depend on retrieval state rather than remain outside the model.
- Item 2: validate or recover true recall order before any move to exact observed-sequence likelihood.
- Item 3: add explicit pre-experimental semantic structure if a later modelling branch wants to go beyond the current binary emotional-versus-neutral scope.
- Item 6: fit a matched general-LPP variant that allows LPP to affect all items.
- Item 11: fit a comparison that isolates context architecture from category-based encoding modulation.
- Item 15: run follow-up analyses aimed at explaining the emotional-item specificity of the LPP-memory relationship mechanistically.
